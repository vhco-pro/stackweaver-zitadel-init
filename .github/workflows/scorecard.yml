# Generated from vhco-pro/.github workflow-templates/scorecard.yml
# See vhco-pro/.github/workflow-templates/scorecard.yml for the source-of-truth template.

name: Scorecard
on:
  workflow_dispatch:
  branch_protection_rule:
  schedule:
    - cron: "21 6 * * 1"
  push:
    branches: [main]


jobs:
  scorecard:
    uses: vhco-pro/.github/.github/workflows/reusable-scorecard.yml@39be64ed0af10ed9076e6129a7b3e7e77b0038c6 # main
    secrets: inherit
    if: github.event.repository.visibility == 'public'
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    with:
      publish_results: true
