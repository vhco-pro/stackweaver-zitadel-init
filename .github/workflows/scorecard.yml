# Generated from vhco-pro/.github workflow-templates/scorecard.yml
# See vhco-pro/.github/workflow-templates/scorecard.yml for the source-of-truth template.

name: Scorecard
on:
  branch_protection_rule:
  schedule:
    - cron: "21 6 * * 1"
  push:
    branches: [main]


jobs:
  scorecard:
    uses: vhco-pro/.github/.github/workflows/reusable-scorecard.yml@420368737f7b821a3ea419dcc30ddcb405c75aaf # main
    if: github.event.repository.visibility == 'public'
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    with:
      publish_results: true
