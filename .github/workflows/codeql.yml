# Generated from vhco-pro/.github workflow-templates/codeql-go.yml

name: CodeQL
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "33 4 * * 2"


jobs:
  analyze:
    if: ${{ github.event.repository.visibility == 'public' }}
    uses: vhco-pro/.github/.github/workflows/reusable-codeql-go.yml@420368737f7b821a3ea419dcc30ddcb405c75aaf # main
    permissions:
      security-events: write
      contents: read
      actions: read
